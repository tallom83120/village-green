$(document).ready(function()
{
	/*$('#categorie').change(function() {
        var cat = $(this).val();
        $.post(
                'https://dev.amorce.org/jmercerol/fil_rouge/index.php/products/sousCategorie',
                'cat_id=' + cat,
                function(data)
                {
                    var contenu = '';
                    $.each(data, function(key, val) {
                        contenu += "<option value='" + val.cat_id + "'>" + val.cat_nom + "</option>";
                    });
                    $("#sous_categorie").html(contenu);
                },
                'JSON'
                );
    });*/
});